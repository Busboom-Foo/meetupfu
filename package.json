{
  "name": "docker-nodeapp",
  "version": "0.20260324.1",
  "private": true,
  "scripts": {
    "dev": "./scripts/dev.sh",
    "test:server": "cd server && npx vitest run",
    "test:client": "cd client && npx vitest run",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "concurrently": "^9.1.2"
  }
}
